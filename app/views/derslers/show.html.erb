<p id="notice"><%= notice %></p>

<article>
<div class="container">
  <div class="row">
    <div class="col-lg-8 col-md-10 mx-auto">
     
      <h2 class="section-heading"> <%= @dersler.baslik %></h2>
      <a href="#">
      <%= image_tag @dersler.resim.url(:medium) if @dersler.resim? %>
      </a>
      <p> <%= @dersler.aciklama %></p>

     
      <p>  <%= @dersler.konu %></p>
      <p>  <%= @dersler.tarih %></p>

<button type="button" class="btn btn-light">  <%= link_to 'Edit', edit_dersler_path(@dersler) %> </button>
<button type="button" class="btn btn-light">  <%= link_to 'Back', derslers_path %></button>

    </div>
  </div>
</div>
</article>

<hr>

